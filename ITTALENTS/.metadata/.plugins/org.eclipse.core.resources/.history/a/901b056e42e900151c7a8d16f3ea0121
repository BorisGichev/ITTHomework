package dao;

import java.sql.Connection;



public abstract class AbstractDAO {
	private final Connection con = (Connection) DBConnection.getInstance().getCon();

	public Connection getCon() {
		return con;
	}
	
}
